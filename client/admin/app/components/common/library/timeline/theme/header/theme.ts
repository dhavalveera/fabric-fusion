import type { TimelineHeaderStyleTypes } from ".";

export const timelineHeaderDefaultValue: TimelineHeaderStyleTypes = {
  styles: {
    base: {
      display: "flex",
      alignItems: "items-center",
      gap: "gap-4",
    },
  },
};
