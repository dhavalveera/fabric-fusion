import type { TimelineStyleTypes } from ".";

export const timelineDefaultValue: TimelineStyleTypes = {
  styles: {
    base: {
      display: "w-full",
      position: "flex",
      flexDirection: "flex-col",
    },
  },
};
